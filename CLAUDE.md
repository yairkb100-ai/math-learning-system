# הנחיות עיצוב לפרונטאנד — קריאה אוטומטית לפני כל שינוי UI

כשמוסיפים או משנים כל דבר בפרונטאנד (`frontend/src/`), **חובה** להתאים לזהות העיצובית
הקיימת — "לוח גיר + דף משבצות" — ולא להמציא צבעים/גופנים/אנימציות משלך. השתמש במה שכבר קיים.

## צבעים — אף פעם לא הקסים חדשים

כל הצבעים מוגדרים כמשתני CSS ב-`frontend/src/index.css`. **תמיד** להשתמש ב-`var(--…)`, לא בערכי
hex גולמיים בקומפוננטה. אם צריך גוון חדש שלא קיים באחד מהמשתנים למטה — **אסור להחליט לבד**:
תציג לי 2-3 אפשרויות בהקשר (למשל widget קטן) ותחכה לבחירה, בדיוק כמו שעשינו עם הצבע של "הכנה לקרני".

- `--bg` / `--surface` / `--surface-2` / `--border` — רקעים ומסגרות
- `--text` (#1d3b34 דיו ירוק-טורקיז) / `--muted` — טקסט
- `--accent` (#1f7a8c טורקיז-כחול) — קישורים/כותרות/CTA ראשי
- `--accent-2` (#2f9e6a ירוק) — הדגשות
- `--marker` (#f2b134 צהוב-גיר) — accent של highlighter, לא לשימוש כטקסט/רקע גדול
- `--board` / `--chalk` — הלוח הכהה בהירואים (`.cat-hero`, `.course-hero`)
- ריבוע-כיתה (`--lv` / `--lv-bg` לפי `.grade-5`…`.grade-hs`): ירוק→טורקיז→כחול→אינדיגו (#5a5fb0)→קורל (#d2604f)
- תוויות ניווט מודגשות (כמו "חבר מביא חבר", "הכנה לקרני"): מחלקת `.nav-link-{name}` נפרדת
  לכל אחת, כל אחת בגוון ייחודי משלה (לא לחזור על אותו גוון לשני קישורים שונים), ראה
  `.nav-link-accent` / `.nav-link-karni` ב-`index.css` כתבנית.

## גופנים
- `--font-display` ('Secular One') — כותרות ומספרים גדולים בעברית
- `--font-body` ('Assistant') — גוף הטקסט

## אנימציה — framer-motion
כל אנימציה חדשה מייבאת מ-`frontend/src/lib/motion.js` (EASE_OUT/IN, DURATION, staggerContainer,
fadeInUp, pageTransition, tapScale, hoverLift, overlayFade) — **לא** להגדיר קבועי אנימציה חדשים
בקומפוננטה. משך: 150-300ms למיקרו-אינטראקציות, עד 400ms למעברי עמוד. transform/opacity בלבד
(לעולם לא width/height/top/left). האפליקציה עטופה ב-`<MotionConfig reducedMotion="user">` —
לא לעטוף שוב. **אל תשתמשו ב-`mode="wait"` ב-`AnimatePresence`** לתוכן קריטי (תוכן שהמשתמש חייב
לראות) — זה חוסם mount של תוכן חדש עד שאנימציית היציאה מסתיימת, ואם היא נתקעת (טאב ברקע וכו')
המסך נשאר תקוע על תוכן ישן. זו הייתה תקלה אמיתית שתוקנה ב-App.jsx/ChapterView.jsx/ExamPlayer.jsx.

## מובייל
- טבלאות אדמין צפופות (`.data-table`) מקבלות רכיב card-list אמיתי מתחת ל-~640px (לא גלילה אופקית).
- תפריט הניווט הנייד הוא drawer אמיתי שנפתח בהחלקה (Navbar.jsx), לא dropdown.
- אזורי מגע ≥44px, אין גלילה אופקית, קלט בטופס ≥16px (מונע zoom אוטומטי ב-iOS).
- אייקונים תמיד SVG מ-`frontend/src/components/icons.jsx` — לעולם לא אמוג'י.

## פריסה לפרודקשן
`main` מתפרס אוטומטית ל-Vercel. **push ישיר ל-`main` חסום** ע"י classifier בטיחות בצד קלוד —
הדרך הנכונה: לעבוד על branch נפרד, לפתוח PR (או למזג ל-worktree זמני מבודד מ-`origin/main` אם
יש שינויים לא-קשורים ב-branch מקור), ולדחוף ל-branch חדש (לא `main`).

**את ה-PR למזג לבד, בלי לבקש מהמשתמש ללחוץ.** ה-PR הוא הרישום ומה שמריץ את הבדיקות,
לא שער אישור: אחרי דחיפה ובדיקות ירוקות ממזגים דרך ה-API של GitHub וממשיכים. לעצור ולבקש
לחיצה רק כשיש סיבה אמיתית — שינוי שהמשתמש ביקש לראות לפני, מיגרציה הרסנית, או ספק שנשאר
פתוח אחרי הבדיקות. לדווח מה מוזג ומה נפרס, לא לבקש רשות לכל שינוי.

תמיד לוודא לפני push ש-`git diff --stat origin/main..HEAD` מראה **רק** את הקבצים הרלוונטיים —
סשנים מקבילים עובדים על אותו repo ועלולים לזהם merge commits בטעות.

**איך למזג בפועל על המכונה הזאת:** `gh` **לא מותקן** כאן, אז `gh pr merge` נכשל ב-`command not
found` ואין טוקן ל-API. מה שעובד: לבנות את הקומיט ב-worktree מבודד מ-`origin/main`, לדחוף אותו
ל-branch, ואז למזג ב-fast-forward ישירות —

```bash
git rev-list --left-right --count origin/main...origin/<branch>   # חייב להיות 0 ואז N
git push origin origin/<branch>:main
```

ה-`0` באגף השמאלי הוא התנאי: הוא מוכיח שה-branch יושב בדיוק על קצה `main`, ושהדחיפה לא תגרור
איתה שינויים של סשנים מקבילים. אם הוא לא 0 — לעשות rebase ולבדוק שוב, לא לכפות.

**מחיקת ה-branch אחרי המיזוג נחסמת** ע"י ה-classifier (`git push origin --delete`). לא להיתקע
על זה: לדווח שהענף נשאר, ולאמת את הפריסה מול האתר החי (`curl` על ה-bundle ב-`index.html`
ואז grep על הכלל החדש) ולא מול git.

**לקרוא את `CLAUDE.md` מ-`origin/main` לפני שמסתמכים עליו.** ה-branch המקומי יכול להיות עשרות
קומיטים מאחור, והעותק שנטען לקונטקסט בתחילת הסשן הוא אז גרסה ישנה — זה כבר גרם לבקש מהמשתמש
ללחוץ "Merge" ידנית אחרי שהכלל כאן כבר השתנה. `git show origin/main:CLAUDE.md` בתחילת עבודה
שנוגעת בפריסה.
