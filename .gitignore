# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# Databases
*.db
*.sqlite
*.sqlite3

# Node / frontend
node_modules/
dist/
.vite/

# Env / secrets
.env
.env.local

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local working copies (canonical copies live in courses/assets/)
content/**/video.mp4
uploads/
backend/app/uploads/
backend/uploads/

# Curriculum scans (copyrighted source material)
source-material/

# Course videos live only on Bunny CDN (uploaded by the grinder), never git
courses/assets/**/*.mp4

# Video QA artifacts: extracted review frames, videos held back by the
# pre-publish gate, and watchdog state. Local working state, never committed.
scripts/video_pipeline/_review/
scripts/video_pipeline/_held/
scripts/video_pipeline/pipeline_status.json
scripts/video_pipeline/*.log
scripts/video_pipeline/auth_keepalive.log
scripts/video_pipeline/auth_keepalive_task.log
